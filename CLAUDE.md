# Devbox

Remote dev environment accessible over Tailscale.

- `scripts/` — session management (start, stop, HTTP server on port 8080)
- `repos/` — git repositories (gitignored)
- `worktrees/` — active session worktrees (gitignored)
- `sessions/` — active session JSON state (gitignored)
